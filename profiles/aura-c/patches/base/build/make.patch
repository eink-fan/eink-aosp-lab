diff --git a/core/main.mk b/core/main.mk
index 04b6b33228..783237e80a 100644
--- a/core/main.mk
+++ b/core/main.mk
@@ -374,7 +374,9 @@ ifneq (,$(user_variant))
   ADDITIONAL_SYSTEM_PROPERTIES += security.perf_harden=1
 
   ifeq ($(user_variant),user)
-    ADDITIONAL_SYSTEM_PROPERTIES += ro.adb.secure=1
+    # Neo 2 M1 temporary DSU diagnostic: make pre-UI USB ADB available while
+    # the generic GSI has no physical e-ink presentation path.
+    ADDITIONAL_SYSTEM_PROPERTIES += ro.adb.secure=0
   endif
 
   ifeq ($(user_variant),userdebug)
diff --git a/target/product/aosp_arm64.mk b/target/product/aosp_arm64.mk
index 6c907db0ed..4b87342d29 100644
--- a/target/product/aosp_arm64.mk
+++ b/target/product/aosp_arm64.mk
@@ -32,6 +32,13 @@
 $(call inherit-product, $(SRC_TARGET_DIR)/product/core_64_bit.mk)
 $(call inherit-product, $(SRC_TARGET_DIR)/product/generic_system.mk)
 
+# Explicit M4 payload-build gate. This adds only the checksummed RM06L
+# runtime files; it does not link or call the vendor presenter.
+# Never set it for M1–M3 builds.
+ifeq ($(NEO2_M4_RUNTIME_PAYLOAD),true)
+$(call inherit-product, frameworks/native/services/surfaceflinger/Neo2Eink/android/vendor-runtime/m4_vendor_runtime_payload.mk)
+endif
+
 # Enable mainline checking for excat this product name
 ifeq (aosp_arm64,$(TARGET_PRODUCT))
 PRODUCT_ENFORCE_ARTIFACT_PATH_REQUIREMENTS := relaxed
